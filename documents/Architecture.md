# Project plan

## Frontend
tauri frameworked vanilla js desktop app.
keeps a list of some form of the software
updates with information from database periodically

Features:
searching, accepting/rejecting software (applications to update to client computers)
clicking on software gives a popup with security information

## Backend

CISO wants a query for softwarex, opens the app an searches the list that updates from the database every <timeinterval>, if no entry is found, it can be requested (AI wrapper).


UI => keeps a list, that loads from 

Current techstack options: 

TAURI tool for RUST backend and vanilla JS frontend